<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/layoutAllMatch"
    android:layout_centerInParent="true"
    android:background="@drawable/background"
    tools:context="com.uottawa.plscuddleme.activities.MainActivity">

    <LinearLayout
        style="@style/layoutAllMatch"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/avatarPic"
            style="@style/signin_img"
            android:src="@drawable/signup_img" />

        <EditText
            android:id="@+id/editTextEmail"
            style="@style/editTextSignInUp"
            android:hint="@string/enter_email"
            android:inputType="textEmailAddress" />

        <EditText
            android:id="@+id/editTextPassword"
            style="@style/editTextSignInUp"
            android:hint="@string/enter_password"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/buttonRegister"
            android:layout_width="wrap_content"
            android:layout_height="50dp"
            android:layout_gravity="center"
            android:text="@string/register" />

        <TextView
            android:id="@+id/textViewLogin"
            style="@style/textViewToggleAccount"
            android:onClick="onClick"
            android:text="@string/existing_member_signin"
            android:textColor="@color/card_text" />
    </LinearLayout>
</RelativeLayout>
